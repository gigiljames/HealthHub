services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: healthhub-backend
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
